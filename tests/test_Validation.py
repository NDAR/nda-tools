import pytest
from NDATools import Validation


def test_init():
    validation = Validation(file_list, config, hide_progress=True, allow_exit=False, thread_num=1)

    # process mocked manifest
    # process mocked csv

    # process mocked calls to validation service

